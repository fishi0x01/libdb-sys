name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build v4.8
        run: cargo build --features v4_8
      - name: Build v5.3
        run: cargo build --features v5_3
